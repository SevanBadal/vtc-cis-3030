module Rating where

-- 1. Define a new Rating type with the constructors One, Two, .. Five. It should derive Ord, Eq and Enum behaviors
-- Make the new type an instance of Show, giving the displays as the appropriate number of stars (i.e., "*", "**" and so on)
-- test: cabal run rating-test -v0